{
  "name": "scanner3d",
  "version": "0.0.1",
  "description": "Scanner 3d app",
  "dependencies": {
    "express": "^4.15.2",
    "fs-exists-sync": "^0.1.0",
    "ftp-client": "^0.2.2",
    "gulp": "^3.9.1",
    "lodash": "^4.17.4",
    "mkdirp": "^0.5.1",
    "socket.io": "^1.7.3",
    "socket.io-stream": "^0.9.1",
    "request": "^2.88.0"
  },
  "scripts": {
    "start": "node index.js",
    "daemon.lint": "cd ./daemon && npm run lint",
    "daemon.install-packages": "cd ./daemon && npm run install-packages"
  },
  "pre-commit": [
    "daemon.lint"
  ],
  "update": [
    "daemon.install-packages"
  ]
}
